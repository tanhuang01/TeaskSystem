<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:useLevel="false">
    <solid android:color="#FFFFE500"></solid>
    <size
        android:width="163dp"
        android:height="60dp" />
    <!-- 线的宽度，颜色灰色 -->
    <!-- 矩形的圆角半径 -->
    <corners android:radius="100dp" />
</shape>